{
  "name": "soundify",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "postinstall": "cd client && npm install && cd ../server && npm install",
    "build": "cd client && npm run build",
    "start": "ls -la && pwd && cd server && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
